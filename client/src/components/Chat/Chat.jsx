import ChatInput from "./ChatInput";
import ChatArea from "./ChatArea";
const Chat = () => {
    return (
        <div className="chat-container">
            <ChatArea />
            <ChatInput />
        </div>
    );
};

export default Chat;
